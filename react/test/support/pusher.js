class Pusher {
  constructor(pusherID) {
    }

  subscribe(type) {
    // console.log('You should not get here and should be stubbing the Stripe.setPublishableKey() method call.');
  }
}

export default Pusher;
